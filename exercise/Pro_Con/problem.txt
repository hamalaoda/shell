场景：  
        两个进程 Producer（生产者）和 Consumer（消费者）
        协作处理一批数据：Producer 生成 100 个整数并写入共享区域，
        Consumer 读取并计算总和。要求数据无冲突（同一时间只有一个进程操作共享区域），
        且 Consumer 需等待 Producer 写入后再读取。

问题：
        如何使用父子进程完成？

答：
        